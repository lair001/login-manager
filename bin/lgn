#!/usr/bin/env python3

import os, sys

PAR_DIR_PATH = os.path.realpath(__file__).replace(__file__.replace(os.curdir, ''), '')
sys.path.insert(0, PAR_DIR_PATH)

from login_manager.LoginManager import LoginManager

if __name__ == "__main__":
    LoginManager().run()
